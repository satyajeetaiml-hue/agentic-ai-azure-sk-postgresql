APP_ENV=local
LOG_LEVEL=INFO

# Set to query a real Azure Database for PostgreSQL (else in-memory mock table).
# e.g. postgresql://user:pass@my-pg.postgres.database.azure.com:5432/orders?sslmode=require
POSTGRES_CONNECTION_STRING=
